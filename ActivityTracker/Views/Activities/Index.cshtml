@{
    ViewData["Title"] = "Index";
}

@if (ViewBag.CurrentUserType == "S")
{
    <h2>My Activities</h2>
}
else if (ViewBag.CurrentUserType == "T")
{
    <h2>@ViewBag.StudentName</h2>
}
else
{
    <text></text>
}


<p>
    <a asp-action="Create" asp-route-studentid="@ViewBag.StudentId">Create New</a>
</p>
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                Activity
            </th>
            <th class="column-sm text-center">
                Fun Factor
            </th>
            <th class="column-sm text-center">
                Difficulty
            </th>
            <th>
                Notes
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in ViewBag.Activities)
        {
            <tr class="@(item.Complete ? "complete" : "")" >
                <td>
                    <a id="overview-activity-name" asp-action="Details" asp-route-id="@item.ID">@item.Name</a>
                </td>
                <td class="text-center">
                    @item.FunFactor
                </td>
                <td class="text-center">
                    @item.Difficulty
                </td>
                <td>
                    @item.Notes
                </td>
                <td>
                    <div class="text-right">
                        <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.ID"><span class="glyphicon glyphicon-trash"></span></a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>